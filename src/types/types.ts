
import { Module, ModuleContent, VisualContent, Question, Quiz, Course } from './index';

// Re-export the types for backward compatibility
export type { Module, ModuleContent, VisualContent, Question, Quiz, Course };
